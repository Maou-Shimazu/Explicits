#pragma once

#include <iostream>
#include <map>
#include <random>
#include <vector>
#include <string>

std::map<std::string, std::string> player; // name, attack, health, 
std::map<std::string, uint16_t> monster; 
std::vector<uint8_t> levels;

const char* player_options =
R"(

)";

